# Current Architecture

The platform uses BatPak for append-only event storage.

Deploys happen on Tuesday after the release meeting.

Ben owns release approval.

The onboarding process uses the current checklist.
